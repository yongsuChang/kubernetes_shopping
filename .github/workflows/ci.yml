name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [ main, develop ]
    paths-ignore:
      - '**.md'

permissions:
  contents: write

jobs:
  build-and-test-backend:
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.tag_version.outputs.new_tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 모든 히스토리를 가져와야 태그 계산이 가능합니다.
      
      - name: Bump version and push tag
        id: tag_version
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch
          tag_prefix: v
          # develop 브랜치일 경우 -develop.x 형태의 pre-release 태그 생성
          append_to_tag: ${{ github.ref == 'refs/heads/develop' && 'develop' || '' }}

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x shopping-backend/gradlew

      - name: Build and Test Backend
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: ./shopping-backend/gradlew -p shopping-backend clean bootJar

      - name: Upload Admin API Artifact
        uses: actions/upload-artifact@v4
        with:
          name: admin-api-jar
          path: shopping-backend/admin-api/build/libs/*.jar

      - name: Upload Shop API Artifact
        uses: actions/upload-artifact@v4
        with:
          name: shop-api-jar
          path: shopping-backend/shop-api/build/libs/*.jar

  frontend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: responsive-react-app/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: responsive-react-app
        run: pnpm install

      - name: Run Frontend Tests
        working-directory: responsive-react-app
        run: pnpm run test -- --run

  docker-build-and-push:
    needs: [build-and-test-backend, frontend-test]
    runs-on: ubuntu-latest
    environment: dockerhub
    steps:
      - uses: actions/checkout@v4

      - name: Download Admin API JAR
        uses: actions/download-artifact@v4
        with:
          name: admin-api-jar
          path: shopping-backend/admin-api/build/libs

      - name: Download Shop API JAR
        uses: actions/download-artifact@v4
        with:
          name: shop-api-jar
          path: shopping-backend/shop-api/build/libs

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Set Docker Tags
        id: docker_meta
        run: |
          VERSION=${{ needs.build-and-test-backend.outputs.new_version }}
          VERSION=${VERSION:-test}
          
          # 기본 태그 설정 (항상 버전 태그 포함)
          ADMIN_TAGS="yongsuchang/shopping-admin-api:${VERSION}"
          SHOP_TAGS="yongsuchang/shopping-shop-api:${VERSION}"
          FRONT_TAGS="yongsuchang/shopping-frontend:${VERSION}"
          
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            # main 브랜치: latest 추가
            ADMIN_TAGS="${ADMIN_TAGS},yongsuchang/shopping-admin-api:latest"
            SHOP_TAGS="${SHOP_TAGS},yongsuchang/shopping-shop-api:latest"
            FRONT_TAGS="${FRONT_TAGS},yongsuchang/shopping-frontend:latest"
          elif [ "${{ github.ref }}" = "refs/heads/develop" ]; then
            # develop 브랜치: test 추가
            ADMIN_TAGS="${ADMIN_TAGS},yongsuchang/shopping-admin-api:test"
            SHOP_TAGS="${SHOP_TAGS},yongsuchang/shopping-shop-api:test"
            FRONT_TAGS="${FRONT_TAGS},yongsuchang/shopping-frontend:test"
          fi
          
          echo "ADMIN_TAGS=${ADMIN_TAGS}" >> $GITHUB_OUTPUT
          echo "SHOP_TAGS=${SHOP_TAGS}" >> $GITHUB_OUTPUT
          echo "FRONT_TAGS=${FRONT_TAGS}" >> $GITHUB_OUTPUT

      - name: Build and Push Admin API
        uses: docker/build-push-action@v5
        with:
          context: ./shopping-backend/admin-api
          push: true
          tags: ${{ steps.docker_meta.outputs.ADMIN_TAGS }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Shop API
        uses: docker/build-push-action@v5
        with:
          context: ./shopping-backend/shop-api
          push: true
          tags: ${{ steps.docker_meta.outputs.SHOP_TAGS }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./responsive-react-app
          push: true
          tags: ${{ steps.docker_meta.outputs.FRONT_TAGS }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  integration-test:
    needs: [build-and-test-backend, docker-build-and-push]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set version
        id: vars
        run: |
          VERSION=${{ needs.build-and-test-backend.outputs.new_version }}
          echo "VERSION=${VERSION:-test}" >> $GITHUB_OUTPUT

      - name: Run Docker Compose
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          TAG: ${{ steps.vars.outputs.VERSION }}
        run: |
          # docker-compose.test.yaml의 이미지를 해당 태그로 변경하여 실행
          sed -i "s/:test/:$TAG/g" docker-compose.test.yaml
          docker compose -f docker-compose.test.yaml pull
          docker compose -f docker-compose.test.yaml up -d

      - name: Wait for services
        run: |
          echo "Waiting for services to be healthy..."
          sleep 40
          docker compose -f docker-compose.test.yaml ps

      - name: Run E2E Test Script
        run: |
          pip3 install requests
          python3 api_test_script.py