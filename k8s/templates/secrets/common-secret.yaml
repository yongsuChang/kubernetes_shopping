apiVersion: v1
kind: Secret
metadata:
  name: shopping-secrets
  namespace: shopping-mall
type: Opaque
stringData:
  # 아래 값들을 실제 환경에 맞게 변경하세요.
  mysql-root-password: "password"
  jwt-secret: "your_jwt_secret_key_value_must_be_very_long_and_secure"
