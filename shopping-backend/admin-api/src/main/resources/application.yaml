spring:
  application:
    name: admin-api
  profiles:
    active: dev
  config:
    import: "aws-parameterstore:/config/shopping/"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  messages:
    basename: classpath*:common-messages,classpath*:admin-messages
    encoding: UTF-8

server:
  port: 8081
 
jwt:
  secret: ${jwt.secret:v9y$B&E)H@McQfTjWnZr4u7x!A%C*F-JaNdRgUkXp2s5v8y/B?E(G+KbPeShVmYq}
  expiration: 3600000 # 1 hour
cors:
  allowed-origins: "http://localhost,http://localhost:5173,http://127.0.0.1:5173"
